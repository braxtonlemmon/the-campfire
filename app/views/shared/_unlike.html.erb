<%= form_for(current_user.likes.find_by(post_id: post_id),
						 html: { method: :delete },
						 remote: true) do |f| %>
	<div><%= hidden_field_tag :post_id, post_id %></div>
	<%= f.submit 'Unlike', class: 'btn btn-sm' %>
<% end %>						 