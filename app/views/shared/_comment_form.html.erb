<%= form_for(post.comments.build) do |f| %>
	<%= f.hidden_field :commentable_id, value: post.id %>
	<%= f.hidden_field :author_id, value: current_user.id %>
  <%= f.text_area :content, placeholder: 'Add a comment here.' %>
	<%= f.submit 'Comment' %>
<% end %>