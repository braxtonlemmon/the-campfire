added a comment
<% if activity.trackable %>
	<strong>"<%= truncate(activity.trackable.content, length: 15) %>"</strong>
	to 
	<% if activity.trackable.commentable.author == current_user %>
		your post
	<% else %>
		<%= activity.trackable.commentable.author.name %>'s post
	<% end %>
	<strong>"<%= truncate(activity.trackable.commentable.content) %>"</strong>
<% else %>
	which has since been removed.
<% end %>
