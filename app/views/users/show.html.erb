<% provide(:title, @user.name) %>

<div class="container">
  <div class='row'>
		<%= avatar_for @user %>
		<h2><%= @user.name.capitalize %></h2>
	</div>
	<% if @user == current_user %>
	  <%= render 'user' %>
	<% else %>
	  <%= render 'other_user'%>
	<% end %>
</div>



