<%= form_for(current_user.friendships.build) do |f| %>
  <div><%= hidden_field_tag :friend_id, locals[:inviter_id] %></div>
	<div><%= hidden_field_tag :user_id, current_user.id %></div>
	<%= f.submit 'Accept', class: 'btn btn-sm' %>
<% end %>